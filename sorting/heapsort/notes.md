Heap: binary tree, parent node whose labeling is 'dominant' over children's labeling. E.g. for min-heap, parent label is smaller than both children.

Data structure for priority queue: extract min & insert.